spring:
  main:
    banner-mode: off

  datasource:
    url : jdbc:mysql://localhost:3306/Community?serverTimezone=UTC&useSSL=false
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
      hibernate:
        ddl-auto: none   # 개발 중에는 create / create-drop / update 중 선택
      properties:
        hibernate:
          format_sql: true
          use_sql_comments: true

  logging:
    logging:
      level:
        root: INFO
        org.springframework.web: DEBUG
        org.springframework.security: DEBUG

server:
  port: 8080

jwt:
  access:
    secret: aGVsbG93b3JsZGhlbGxvd29ybGRoZWxsb3dvcmxk
    expiration: 15m
  refresh:
    secret: cmVmcmVzaHNlY3JldGtleWZvcmp3dHRva2Vu
    expiration: 7d
